% waypoints: keep list of waypoints (matrix 2*N)
%
% usage:
% ww = waypoints(W);		% create waypoint array
% distance(ww,x)		% distance of first waypoint to x (2D)
% truncate(ww)			% remove first waypoint from list
%
% Alle-Jan van der Veen, TU Delft, 3 June 2020
